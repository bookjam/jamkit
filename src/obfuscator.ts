import obfuscator from "javascript-obfuscator";

interface ObfuscatorModule {
    obfuscate(): void;
}

const obfuscatorModule: ObfuscatorModule = {
    obfuscate(): void {
        // Obfuscation implementation (currently empty)
    }
};

export default obfuscatorModule;